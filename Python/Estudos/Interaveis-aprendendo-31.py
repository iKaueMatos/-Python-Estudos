#Interavel e um objeto que você pode interar sobre ele 
# mais necessariamente ele e um interador

lista = [0,1,2,3,4,5,6]

#Função hasattr:Os argumentos são um objeto e uma string. O resultado é True se a string é o nome de um dos atributos do objeto, e False se ela não for. (Isto é implementado chamando getattr(object, name) e vendo se levanta um AttributeError ou não.)

